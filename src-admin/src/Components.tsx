import RemoteControl from './RemoteControl';

// The key here is the last segment of `name` in admin/jsonConfig.json:
// "ConfigCustomLgTvSet/Components/RemoteControl"
export default { RemoteControl };
